//#include "stdafx.h"
#include <iostream>

using namespace std;

int main()
{
	int a, b;
	//用hex控制输入为十六进制，方便许多，此时的我做这题，陷入大坑
	while (cin >> hex >> a >> hex >> b)
	{
		cout << a + b << endl;
	}
	return 0;
}
